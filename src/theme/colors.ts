// Deprecated: palette tokens were migrated into MUI's theme.palette
// Keep a minimal stub to avoid accidental breakage while migrating.
export const COLORS = {} as const;

export type ColorKeys = never;

export default COLORS;
